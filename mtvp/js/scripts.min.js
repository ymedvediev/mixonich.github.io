function doIt() {
    var getValue = $('#myTable td:nth-child(2) input').map(function () {
        return $(this).val();
    });
    var setValue = $('#myTable td:nth-child(1) input').map(function () {
        return $(this).val();
    });
    var absValue = Array();
    for (var i = 0; i < getValue.length; i++) {
        absValue[i] = Math.abs(setValue[i] - getValue[i]);
    }
    var vidValue = Array();
    for (var i = 0; i < getValue.length; i++) {
        vidValue[i] = (absValue[i] / setValue[i]).toFixed(4);
    }
    var max = 0,
        min = 10 * 100;
    for (var i = 0; i < getValue.length; ++i) {
        if (getValue[i] > max) max = getValue[i];
        if (getValue[i] < min) min = getValue[i];
    }
    var prValue = Array();
    for (var i = 0; i < getValue.length; i++) {
        prValue[i] = (absValue[i] / (max - min)).toFixed(4);
    }

    var counta = 0;
    $("tr").each(function (index) {
        $(this).find("td:eq(2)").append(function () {
            for (var i = 0; i < getValue.length; i++) {
                counta++;
                return absValue[counta-1];
            }
        });
    });
    var countv = 0;
    $("tr").each(function (index) {
        $(this).find("td:eq(3)").append(function () {
            for (var i = 0; i < getValue.length; i++) {
                countv++;
                return vidValue[countv - 1];
            }
        });
    });
    var countp = 0;
    $("tr").each(function (index) {
        $(this).find("td:eq(4)").append(function () {
            for (var i = 0; i < getValue.length; i++) {
                countp++;
                return prValue[countp - 1];
            }
        });
    });
}

function addRow(id) {
    var tbody = document.getElementById(id).getElementsByTagName("TBODY")[0];
    var row = document.createElement("TR")
    var td1 = document.createElement("TD")
    var td2 = document.createElement("TD")
    var td3 = document.createElement("TD")
    var td4 = document.createElement("TD")
    var td5 = document.createElement("TD")
    var inputVal1 = document.createElement("INPUT")
    var inputVal2 = document.createElement("INPUT")
    td1.appendChild(inputVal1);
    td2.appendChild(inputVal2);
    row.appendChild(td1);
    row.appendChild(td2);
    row.appendChild(td3);
    row.appendChild(td4);
    row.appendChild(td5);
    tbody.appendChild(row);
}